{
  "name": "@teos/bankchain-android",
  "version": "1.0.0",
  "description": "TEOS Bankchain - Android App",
  "scripts": {
    "build": "cd ../../frontend/web && npm run build",
    "sync": "npm run build && npx cap sync android",
    "open": "npx cap open android",
    "run": "npm run sync && npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^5.5.0",
    "@capacitor/core": "^5.5.0",
    "@capacitor/keyboard": "^5.0.6",
    "@capacitor/splash-screen": "^5.0.6",
    "@capgo/capacitor-native-biometric": "^5.0.0",
    "@capacitor-community/secure-storage": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.5.0"
  }
}
