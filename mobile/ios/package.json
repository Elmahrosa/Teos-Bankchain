{
  "name": "@teos/bankchain-ios",
  "version": "1.0.0",
  "description": "TEOS Bankchain - iOS App",
  "scripts": {
    "build": "cd ../../frontend/web && npm run build",
    "sync": "npm run build && npx cap sync ios",
    "open": "npx cap open ios",
    "run": "npm run sync && npx cap run ios"
  },
  "dependencies": {
    "@capacitor/ios": "^5.5.0",
    "@capacitor/core": "^5.5.0",
    "@capacitor/keyboard": "^5.0.6",
    "@capacitor/splash-screen": "^5.0.6",
    "@capgo/capacitor-native-biometric": "^5.0.0",
    "@capacitor-community/secure-storage": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.5.0"
  }
}
